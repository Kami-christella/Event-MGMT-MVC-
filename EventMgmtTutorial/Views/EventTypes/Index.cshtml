@model IEnumerable<EventMgmtTutorial.Models.EventType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
   Event Types
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.typeEvent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.typeEvent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.uuid }) |
            @Html.ActionLink("Details", "Details", new { id=item.uuid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.uuid })
        </td>
    </tr>
}

</table>
